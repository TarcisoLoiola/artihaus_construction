# .env.development (used when running `npm start`)
REACT_APP_API_URL=http://localhost:5000/api

# .env.production (used when running `npm run build`)
REACT_APP_API_URL=https://artihaus.onrender.com/api

